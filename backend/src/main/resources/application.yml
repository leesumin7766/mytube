spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mytubedb
    username: root
    password: terry8473   # 👉 실제 MariaDB root 비밀번호로 변경
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create / update / validate / none 중 선택
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379

server:
  port: 8080
